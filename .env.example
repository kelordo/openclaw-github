# Example environment file for local development.
# NOTE: pr-autopilot currently does NOT auto-load .env files.
# Export these variables in your shell (or use a tool like dotenvx) before running.

# GitHub personal access token or fine-grained token.
# Required for GitHub API-backed commands.
GITHUB_TOKEN=
